# Python
__pycache__/
*.py[cod]
*$py.class
build/
dist/

# Virtual Environment
venv/
ENV/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
Thumbs.db
desktop.ini

# Project specific
# Exclude snapshots folder (contains user-specific data extracts)
Vribbels/snapshots/

# User-state folder. The bundled defaults live in `default_settings/`
# (tracked) and are merged into `settings/` on first run by
# defaults_sync.py. Never track the user's `settings/` -- it holds
# their own presets, char assignments, optimizer config, etc.
Vribbels/settings/

# Claude Code files and internal documentation
.claude/
CLAUDE.md
plan.md
tasks.md
paused_task.md
docs/
past_plans/

# Git worktrees (isolated workspaces for parallel development)
.worktrees/

# Build files and tools (not needed in repository).
# `build/` and `dist/` are covered by the Python block above.
# The .spec is regenerated by every `zCreate exe.bat` run.
*.spec
zCreate exe.bat

# I'm normal
.old/
_tmp/
zRUN.bat
zRUN Checks.bat
zRUN Spacing Audit.bat
zRUN Spacing Audit Verbose.bat
zRUN Spacing Audit Freeze.bat