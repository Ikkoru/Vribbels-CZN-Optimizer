# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environment
venv/
ENV/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Project specific
# Exclude snapshots folder (contains user-specific data extracts)
Vribbels/snapshots/

# User-state folder. The bundled defaults live in `default_settings/`
# (tracked) and are merged into `settings/` on first run by
# defaults_sync.py. Never track the user's `settings/` -- it holds
# their own presets, char assignments, optimizer config, etc.
Vribbels/settings/

# Reference images and icon generation tools - not needed in repository
CZN Assets/
/images/
Vribbels/images/create_icon.py
Vribbels/images/generate_growth_stones.py
Vribbels/images/__pycache__/

# Claude Code files and internal documentation
.claude/
CLAUDE.md
RELEASE_CHECKLIST.md
PLAN.md
docs/

# Git worktrees (isolated workspaces for parallel development)
.worktrees/

# Build files and tools (not needed in repository)
build/
dist/
build.bat
build.spec
BUILD.md
*.spec
zCreate exe.bat

# Release packages (distributed via GitHub Releases)
*.zip
Vribbels/Vribbels.zip

# I'm normal
zRUN.bat
Vribbels/default_settings/_don't forget to make excluded_default_initialized false